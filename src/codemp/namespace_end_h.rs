#![allow(non_snake_case)]

// C origin: `codemp/namespace_end.h`.
// On Xbox builds this closed a C++ namespace and imported it with `using`.
// Rust has no equivalent include-scope namespace close/import operation.
