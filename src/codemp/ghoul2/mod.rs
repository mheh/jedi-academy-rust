//! Ported `codemp/ghoul2/` sources — the ghoul2 model/skeleton headers the game
//! module shares with the engine. Mirrors the upstream `refs/raven-jediacademy/codemp/ghoul2/`
//! tree, one Rust file per C file (`g2.h` → `g2_h.rs`).

pub mod g2_h;
