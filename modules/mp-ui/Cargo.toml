[package]
name = "ui"
version = "0.1.0"
edition = "2021"
rust-version = "1.77"
license = "GPL-2.0-only"
description = "Jedi Academy MP UI module (ui)"

[lib]
name = "ui"
path = "src/lib.rs"
crate-type = ["cdylib", "rlib"]

[features]
oracle = []
vm = []
